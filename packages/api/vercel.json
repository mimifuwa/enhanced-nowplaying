{
  "buildCommand": "cd ../.. && pnpm --filter=api build",
  "outputDirectory": "packages/api/.next",
  "installCommand": "cd ../.. && pnpm --filter=api install",
  "framework": "nextjs",
  "regions": ["hnd1"],
  "functions": {
    "src/app/api/**/*.{js,ts}": {
      "maxDuration": 10
    }
  }
}
