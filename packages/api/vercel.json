{
  "buildCommand": "cd ../.. && pnpm --filter=api build",
  "outputDirectory": "packages/api/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "regions": ["hnd1"],
  "functions": {
    "src/app/api/**/*.{js,ts}": {
      "maxDuration": 10
    }
  }
}
